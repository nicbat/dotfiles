[Appearance]
# Colors come from Noctalia's template output. Noctalia rewrites
# colors/noctalia.conf on every wallpaper/palette change, so this
# pointer stays correct and the palette stays dynamic.
color_scheme_path=/home/nicbat/.config/qt5ct/colors/noctalia.conf
custom_palette=true
style=Fusion
icon_theme=Papirus-Dark
standard_dialogs=default
